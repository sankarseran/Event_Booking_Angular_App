import { PreventKeyInputDirective } from './prevent-key-input.directive';

describe('PreventKeyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventKeyInputDirective();
    expect(directive).toBeTruthy();
  });
});
